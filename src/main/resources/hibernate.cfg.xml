<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- datasource config -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>

        <property name="hibernate.connection.url">jdbc:postgresql://ep-flat-cherry-a9nn5y4n.gwc.azure.neon.tech/PrzydasieDB</property>
        <property name="hibernate.connection.username">PrzydasieDB_owner</property>
        <property name="hibernate.connection.password">qnpg_RjqFmOfzn6h0</property>


        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Enable Hibernate's automatic session context management -->
        <property name="current_session_context_class">thread</property>

        <!-- Disable the second-level cache  -->
        <property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <property name="hbm2ddl.auto">update</property>

        <mapping class="org.example.model.Admin"/>
        <mapping class="org.example.model.Auction"/>
        <mapping class="org.example.model.Buyer"/>
        <mapping class="org.example.model.Item"/>
        <mapping class="org.example.model.PremiumUser"/>
        <mapping class="org.example.model.Seller"/>
        <mapping class="org.example.model.User"/>

    </session-factory>
</hibernate-configuration>